import { Box } from "@mui/material";
import React from "react";
import AddressPage from "../components/address_page/AddressPage";

const RoutingPage = () => {
  return (
    <Box>
      <AddressPage />
    </Box>
  );
};

export default RoutingPage;
